@page
@model Learn_Academy.Pages.Courses.IndexModel
@inject Microsoft.AspNetCore.Identity.SignInManager<Learn_Academy.Models.ApplicationUser> signInManager;
@section Scripts
{
    <script src="@Url.Content("https://kit.fontawesome.com/9c3fdd9015.js")"></script>
    <script src="@Url.Content("https://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js")"></script>
    <script src="@Url.Content("https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/2.3.1/js/bootstrap.min.js")"></script>


}
@{
    ViewData["Title"] = "Index";
}
<style>
    .body {
        overflow-x: hidden;
    }

    .custom-wrap-details {
        justify-content: space-around;
        color: white;
        margin-top: 10px;
    }

        .custom-wrap-details h2 {
            font-size: 20px;
        }

    .draft-container-wrap {
        margin-bottom: 10vh;
        display: block;
    }

    .draft-container {
        position: relative;
        display: inline-block;
    }

    .draft-image {
        width: 98vw;
        min-height: 200px;
    }

    .draft-text {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate( -50%, -50% );
        color: white;
    }

        .draft-text h1, .draft-text h3 {
            font-family: 'Poppins', sans-serif;
            font-weight: 700;
            text-align: center;
            text-shadow: -1px -1px 0 #000, 1px -1px 0 #000, -1px 1px 0 #000, 1px 1px 0 #000;
        }

        .draft-text h1 {
            font-size: 5rem;
        }

    .custom-search-bar {
        min-width: 45vw;
    }

    .card {
        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
        transition: 0.3s;
        width: 40%;
        max-width: 300px;
        max-height: 400px;
        min-width: 300px;
        max-height: 400px;
        margin-left: 10px;
    }

        .card:hover {
            box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
        }

    .container {
        padding: 2px 16px;
    }

    .container-courses-wrap {
        text-align: center;
        width: 58vw;
        overflow: scroll;
        overflow-y: hidden;
    }
</style>

<div id="banner" class="draft-container-wrap">
    <div class="draft-container">
        <img class="draft-image img-fluid" src="https://images.unsplash.com/photo-1485546246426-74dc88dec4d9?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1350&q=80" alt="Experience" />
        <div class="draft-text">
            <div class="custom-wrap-Titles">
                <h1 style="font-size: 40px;">
                    The World's Best Online Learning Platform <br />
                    Only at Learn Academy
                </h1>
            </div>
            <form method="get">
                <div class="input-group custom-search-bar">
                    <input placeholder="Search..." class="form-control" asp-for="searchCourses" />
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-primary"><i class="fa fa-search"></i></button>
                    </div>
                </div>
            </form>
            <div class="d-flex custom-wrap-details">
                <h2 style="color:yellow;">More than 10,000 Courses</h2>
                <h2 style="color:turquoise;">Over 300,000 Students</h2>
                <h2 style="color:red;">Start Learning Now</h2>
            </div>
        </div>
    </div>
</div>
<!--
<p>
    <a asp-page="Create">Create New</a>
</p>
-->

<!--
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].CourseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Course[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Course)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CourseDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                    <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
-->
<div class="d-flex container-courses-wrap">
    @foreach (var item in Model.Course)
    {
        <div class="card">
            <img src="https://st.depositphotos.com/1364311/1336/i/950/depositphotos_13360079-stock-photo-girl-studying-in-library.jpg" alt="Avatar" style="        width: 100%
">
            <div class="container">
                <h4><b>@Html.DisplayFor(modelItem => item.Name)</b></h4>
                <p>@Html.DisplayFor(modelItem => item.Author)</p>
                <p style="text-decoration:line-through;">@Html.DisplayFor(modelItem => item.Price)</p>
                <!-- Change this to updated price -->
                <p style="font-size: 2em;">@Html.DisplayFor(modelItem => item.Price)</p>
                @if (signInManager.IsSignedIn(User) && User.IsInRole("Admin"))
                {
                    <div>
                        <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
                    </div>
                }

            </div>
        </div>
    }
</div>